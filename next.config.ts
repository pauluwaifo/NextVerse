import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['openweathermap.org', 'api.weatherapi.com', 'img.spoonacular.com', 'cdn2.thedogapi.com', 'cdn2.thecatapi.com'],  
  },
};

export default nextConfig;
